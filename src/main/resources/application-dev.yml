#
# Development Environmentsserver:
server:
  port: 9000
spring:

#  datasource:
#    continue-on-error: false
#  h2:
#    console:
#      enabled: true
#      path: /h2
#    database:
#      url: jdbc:h2:mem:demo;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#      username: sa
#      password:
#      driver-class-name: org.h2.Driver
#      platform: h2
#      initialize: true
#    hibernate:
#      dialect: org.hibernate.dialect.H2Dialect
#      ddl-auto: create-drop

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    name: postgresdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    root: ERROR
    com.ravuthz.springboot.core.DatabaseLoader: DEBUG